export function ClampMul(x: number, y: number): number {
  return x * y;
}

export function ClampSub(x: number, y: number): number {
  return x - y;
}

export function ClampAdd(x: number, y: number): number {
  return x + y;
}
